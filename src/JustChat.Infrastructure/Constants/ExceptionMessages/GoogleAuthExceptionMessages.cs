namespace JustChat.Infrastructure.Constants.ExceptionMessages;

public static class GoogleAuthExceptionMessages
{
    public const string ClientIdsNotConfigured =
        "Google authentication is not configured: GoogleAuth:ClientIds must contain at least one OAuth client ID.";
}
